# {{ ansible_managed }}
127.0.0.1	localhost
127.0.1.1	ubuntu

::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

{% set data = (inventory.stdout|from_json) %}
{% for host in data.all.hosts %}
{{ data._meta.hostvars[host]['ip_address'] }} {{ host }}
{% endfor %}
